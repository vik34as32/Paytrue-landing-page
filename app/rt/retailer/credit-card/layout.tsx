"use client";

import CcbpShell from "@/src/modules/ccbp/components/CcbpShell";

export default function CreditCardLayout({ children }: { children: React.ReactNode }) {
  return <CcbpShell>{children}</CcbpShell>;
}
